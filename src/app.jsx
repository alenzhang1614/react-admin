import React,{Component} from 'react';
import {Button} from 'antd'//引入antd库
export default class App extends Component{
    render() {
        return(
            <Button type="primary">按钮</Button>
        )
    }
}